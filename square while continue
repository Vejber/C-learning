int is_square(int k){
int x=0;
    while(k>=(x*x))
           {if (k>(x*x))
           {x=x+1; continue;}
            else return 1;
           }

    return 0;
    
}/*введите число (16), х=0. До тех пор, пока 16>=x*x, если 16>x*x, добавляй х+1 и снова проверяй выполнение лупа. В случае, если 16=х*x, верни 1. Если луп не выполняется, верни 0*/
