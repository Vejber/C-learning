void divisors(int a)
{
printf("\n%d: ", a);
int b;
for(b = 2; b <= a; b=b+1)
    if(a % b == 0) printf("%d ", b);
}
 
void all_divisors(int limit)
{
int b;
for(b = 1; b <=limit; b=b+1)
    divisors(b);
}
 
int main()
{
all_divisors(100);
return 0;
}
